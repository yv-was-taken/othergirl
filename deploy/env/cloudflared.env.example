# Tunnel token from Cloudflare Zero Trust > Networks > Tunnels > your tunnel > Install and run connector.
# Place the raw token value in /etc/othergirl/tunnel-token (no trailing newline).
# The systemd unit loads it via LoadCredential and passes it to cloudflared
# through the TUNNEL_TOKEN environment variable to avoid process-list exposure.
